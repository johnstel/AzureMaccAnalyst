# Agents

Place one instruction file per agent role.

Recommended convention:
- `<agent-name>.instructions.md`

Domains already scaffolded:
- `azure-devops/`
- `cost-intelligence/`
- `orchestration/`
- `shared/`

Use `shared/` for common guardrails and style requirements that multiple agents should inherit.
