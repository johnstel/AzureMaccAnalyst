@using AzureMaccAnalyst.Models

<div class="table-responsive mt-3">
    <table class="table table-sm table-striped table-hover">
        <thead>
            <tr>
                <th>Resource Type</th>
                <th class="text-end">Current 3-Yr Cost</th>
                <th class="text-end">RI/SP 3-Yr Cost</th>
                <th class="text-end">Net Savings (3 Yr)</th>
                <th class="text-end">Savings %</th>
                <th class="text-end">Annual Savings</th>
                <th class="text-end">Monthly Savings</th>
                <th class="text-end">% of Total</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var row in Rows)
            {
                <tr>
                    <td>@row.ResourceType</td>
                    <td class="text-end">@Currency @row.Current3YrCost.ToString("N2")</td>
                    <td class="text-end">@Currency @row.RiSp3YrCost.ToString("N2")</td>
                    <td class="text-end text-success fw-bold">@Currency @row.NetSavings3Yr.ToString("N2")</td>
                    <td class="text-end">@row.SavingsPercent.ToString("N1")%</td>
                    <td class="text-end">@Currency @row.AnnualSavings.ToString("N2")</td>
                    <td class="text-end">@Currency @row.MonthlySavings.ToString("N2")</td>
                    <td class="text-end">@row.PercentOfTotalSavings.ToString("N1")%</td>
                </tr>
            }
        </tbody>
        @if (Rows.Any())
        {
            <tfoot>
                <tr class="fw-bold">
                    <td>Total</td>
                    <td class="text-end">@Currency @Rows.Sum(r => r.Current3YrCost).ToString("N2")</td>
                    <td class="text-end">@Currency @Rows.Sum(r => r.RiSp3YrCost).ToString("N2")</td>
                    <td class="text-end text-success">@Currency @Rows.Sum(r => r.NetSavings3Yr).ToString("N2")</td>
                    <td></td>
                    <td class="text-end">@Currency @Rows.Sum(r => r.AnnualSavings).ToString("N2")</td>
                    <td class="text-end">@Currency @Rows.Sum(r => r.MonthlySavings).ToString("N2")</td>
                    <td></td>
                </tr>
            </tfoot>
        }
    </table>
</div>

@code {
    [Parameter] public List<SavingsCategoryRow> Rows { get; set; } = new();
    [Parameter] public string Currency { get; set; } = "USD";
}
